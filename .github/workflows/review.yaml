name: shell commands 

on:
  - push

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v1 
      - name: echo string
        run: echo "Hello"
      - name: Multi line string
        run: |
          pwd
          node -v
          ls -a
      